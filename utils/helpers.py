import streamlit as st

def local_css(file_name):
    """
    Load local CSS (not used in this implementation as we're adhering to Streamlit's default styling).
    This function is included for possible future use if styling needs change.
    
    Args:
        file_name (str): CSS file path
    """
    # This function is included for potential future use
    # Currently not being used as we're following the guideline to use Streamlit's default styling
    pass
